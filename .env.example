# Client-side example only. Never put real provider secrets in the Android APK.
# Real provider secrets belong on the backend / Supabase Edge Function secret store.

GEMINI_API_KEY=MY_GEMINI_API_KEY

# Backend-only secrets (DO NOT ship these in the APK):
# OPENAI_API_KEY=...
# OPENAI_TEXT_MODEL=gpt-5.6-luna
# FIREBASE_STORAGE_BUCKET=...
# ALLOWED_ORIGINS=*
